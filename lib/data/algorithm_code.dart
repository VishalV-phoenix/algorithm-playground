const linearSearchCode = [
  "",
  "for (int i = 0; i < arr.length; i++)",
  "if (arr[i] == target)",
  "return i",
];

const binarySearchCode = [
  "",
  "while (low <= high)",
  "if (arr[mid] == target)",
  "return mid",
  "low = mid + 1",
  "high = mid - 1",
];